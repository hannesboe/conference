type Conference {
  id: ID!
  name: String
  startDate: String
  endDate: String
  locations: [Location] @relation(name: "TAKES_PLACE", direction: "BOTH")
}

type Location {
  name: String
  address: String
  state: String
  city: String
}

